{
  "name": "uncharted-territory-backend",
  "version": "1.0.0",
  "description": "Backend worker for Uncharted Territory",
  "main": "src/index.ts",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --format=esm --outfile=dist/index.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230419.0",
    "esbuild": "^0.19.2",
    "typescript": "^5.0.4",
    "vitest": "^0.31.0",
    "wrangler": "^3.0.0"
  }
}